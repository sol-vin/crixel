name: crixel
version: 0.1.0

authors:
  - Ian Rash <ian@sol.vin>

targets:
  sandbox:
    main: src/sandbox.cr

dependencies:
  minievents:
    github: sol-vin/minievents
crystal: '>= 1.11.2'

license: MIT